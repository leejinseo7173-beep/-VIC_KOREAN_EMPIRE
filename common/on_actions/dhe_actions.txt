# Encoding: UTF-8 with BOM
# File: common/on_actions/dhe_on_actions.txt
# [수정] 글로벌 쿨다운 추가 → 전체 Phase 통틀어 2~3개월에 1개만 발동

on_monthly_pulse_country = {
	effect = {
		if = {
			limit = {
				exists = c:KOR
				c:KOR ?= THIS
				NOT = { has_variable = dhe_ginseng_obsessions_added }
			}

			dhe_add_ginseng_obsessions_effect = yes
			set_variable = dhe_ginseng_obsessions_added
		}

		# 월간 범용 랜덤 이벤트는 정리 대상이라 발동하지 않는다.
		# 중추원/동경총독부 랜덤 사건은 각 일지의 random_events 블록에서만 처리한다.
	}
}
